apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: disallow-run-as-root
  annotations:
    policies.kyverno.io/title: Disallow running as root
    policies.kyverno.io/category: Security
    policies.kyverno.io/severity: high
    policies.kyverno.io/subject: Pod, Deployment
    policies.kyverno.io/description: >-
      Most container breakout CVEs require the process to be root (UID 0).
      Non-root containers are significantly harder to exploit. This policy
      flags any container with runAsUser:0 or runAsNonRoot:false.
spec:
  validationFailureAction: Audit
  background: true
  rules:
    - name: deny-run-as-root-deployment
      match:
        any:
          - resources:
              kinds:
                - Deployment
              namespaces:
                - demo
      validate:
        message: "Containers must not run as root. Set runAsNonRoot:true and runAsUser to a non-zero UID."
        pattern:
          spec:
            template:
              spec:
                containers:
                  - =(securityContext):
                      =(runAsUser): ">0"
                      =(runAsNonRoot): true

    - name: deny-run-as-root-pod
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - demo
      validate:
        message: "Containers must not run as root. Set runAsNonRoot:true and runAsUser to a non-zero UID."
        pattern:
          spec:
            containers:
              - =(securityContext):
                  =(runAsUser): ">0"
                  =(runAsNonRoot): true
